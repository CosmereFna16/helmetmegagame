-- A bound book: the fourth PaperKind. Same row shape as a written sheet, but
-- its text is fixed at binding rather than appended to (docs/systemdocs/PAPERWORK.md).
ALTER TYPE "PaperKind" ADD VALUE 'BOOK';
